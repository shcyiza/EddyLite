<%= form_with(model: sale, local: true) do |form| %>
  <% if sale.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sale.errors.count, "error") %> prohibited this sale from being saved:</h2>

      <ul>
        <% sale.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :saleable_id %>
    <%= form.text_field :saleable_id %>
  </div>

  <div class="field">
    <%= form.label :transaction_type %>
    <%= form.text_field :transaction_type %>
  </div>

  <div class="field">
    <%= form.label :label_revenue %>
    <%= form.text_field :label_revenue %>
  </div>

  <div class="field">
    <%= form.label :artist_revenu %>
    <%= form.text_field :artist_revenu %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
